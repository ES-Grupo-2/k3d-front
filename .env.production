# URL pública da API (backend Fastify no Railway, sob o subdomínio de produção).
# NEXT_PUBLIC_* é embutida no build — precisa estar presente no build de produção.
NEXT_PUBLIC_API_URL=https://api.k3d.eyepleasure.com.br
